String sayHello(String name, int age, String country){
 return "Hello %name, you are $age, and you come from $country";}

 void main() {
   print(sayHello('gghwan', 19, 'korea'));
   }


   #case 2
   // named parameters -> 까먹을 수 있기 때문에. order에 연연할 필요 없음.

void main() {
  print(sayHello(
    age:19,
    name:'gghwan',
    country:'korea'
    ));
    }

    //dart 엔 nullsafety가 존재한다. 만약 age, name, country모두 적지 않는다면?
    //1. defaultValue를 정해줄 수 있다.
  
    /* required String name,
       required int age,
       reguired String country */
       // required modifier -> parameter에서 호출된다는걸 dart에게 알려주는 역할을 한다.
